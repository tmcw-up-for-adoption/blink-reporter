{
    "name": "blink-reporter",
    "version": "0.0.0",
    "description": "a mocha reporter that changes a blink(1)",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "license": "BSD",
    "dependencies": {
        "node-blink": "0.0.2"
    }
}
